services:
  frontend-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kle-blog-frontend-app
    ports:
      - "8001:8000"
      - "5173:5173"
    volumes:
      - .:/var/www
    networks:
      - kle-blog-network

networks:
  kle-blog-network:
    external: true